void getDeviceCode(); 

extern vector<string> parallel_array;   // the name of the array been mapped
extern vector<string> parallel_array1;   // the name of the array been mapped
extern vector<string> parallel_function; // the function used to map
extern vector<string> parallel_command; // e.g. map array add
extern vector<string> parallel_output; // for reduce
